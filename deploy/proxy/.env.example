# Domain name with an A record pointing to the VM (e.g., school.example.com).
DOMAIN=your-domain.com

# Email for Let's Encrypt registration and expiry notices.
CERT_EMAIL=you@example.com

# Docker network created by the application stack (deploy/compose.yaml).
# Verify with: docker network ls | grep school
APP_NETWORK=school-platform_default
